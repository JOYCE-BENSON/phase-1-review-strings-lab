// Write your code in this file!
const currentUser = 'Grace Hopper';  // Define your currentUser here

const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;  // Concatenate the greeting with currentUser

const excitedWelcomeMessage = welcomeMessage.toUpperCase();  // Convert the welcome message to uppercase

const shortGreeting = `Welcome, ${currentUser.slice(0, 1)}!`;  // Get the first initial of currentUser

